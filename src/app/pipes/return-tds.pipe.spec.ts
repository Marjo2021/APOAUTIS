import { ReturnTdsPipe } from './return-tds.pipe';

describe('ReturnTdsPipe', () => {
  it('create an instance', () => {
    const pipe = new ReturnTdsPipe();
    expect(pipe).toBeTruthy();
  });
});
